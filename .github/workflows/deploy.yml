# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: deploy
'on':
  workflow_dispatch: {}
jobs:
  provision:
    runs-on: ubuntu-latest
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
      TF_VAR_my_ip: ${{ secrets.MY_IP }}
    timeout-minutes: 20
    outputs:
      instance_ip: ${{ steps.tf_outputs.outputs.instance_ip }}
      instance_id: ${{ steps.tf_outputs.outputs.instance_id }}
    steps:
    - uses: actions/checkout@v4
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.7.5
    - name: Terraform Init
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\n"
      working-directory: infra
    - name: Terraform Apply
      run: terraform apply -input=false -auto-approve
      working-directory: infra
    - id: tf_outputs
      name: Get Outputs
      run: 'echo "instance_ip=$(terraform output -raw instance_public_ip)" >> "$GITHUB_OUTPUT"

        echo "instance_id=$(terraform output -raw instance_id)" >> "$GITHUB_OUTPUT"

        '
      working-directory: infra
  configure:
    runs-on: ubuntu-latest
    needs:
    - provision
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
      TF_VAR_my_ip: ${{ secrets.MY_IP }}
    timeout-minutes: 20
    steps:
    - uses: actions/checkout@v4
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.7.5
    - id: tf_ip
      name: Read instance IP from state
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\necho\
        \ \"instance_ip=$(terraform output -raw instance_public_ip)\" >> \"$GITHUB_OUTPUT\"\
        \n"
      working-directory: infra
    - name: Install Ansible
      run: pip install ansible-core
    - name: Write SSH private key
      run: 'mkdir -p ~/.ssh

        printf ''%s\n'' "$SSH_KEY" > ~/.ssh/deploy_key

        chmod 600 ~/.ssh/deploy_key

        '
      env:
        SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
    - name: Wait for SSH port 22
      run: "HOST=\"${{ steps.tf_ip.outputs.instance_ip }}\"\nfor i in $(seq 1 30);\
        \ do\n  if nc -zw5 \"$HOST\" 22; then\n    echo \"SSH port is open after $i\
        \ attempt(s)\"\n    exit 0\n  fi\n  echo \"Attempt $i: SSH not ready, waiting\
        \ 10s...\"\n  sleep 10\ndone\necho \"ERROR: SSH port 22 never became reachable\
        \ on $HOST\"\nexit 1\n"
    - name: Scan host key
      run: ssh-keyscan -H "${{ steps.tf_ip.outputs.instance_ip }}" >> ~/.ssh/known_hosts
    - name: Generate inventory
      run: 'cat > /tmp/inventory.ini <<EOF

        [vault]

        ${{ steps.tf_ip.outputs.instance_ip }} ansible_user=${{ secrets.SSH_USER }}
        ansible_ssh_private_key_file=~/.ssh/deploy_key

        EOF

        '
    - name: Run Ansible playbook
      run: ansible-playbook -i /tmp/inventory.ini ansible/site.yml
  verify:
    runs-on: ubuntu-latest
    needs:
    - provision
    - configure
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
      TF_VAR_my_ip: ${{ secrets.MY_IP }}
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@v4
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.7.5
    - id: tf_ip
      name: Read instance IP from state
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\necho\
        \ \"instance_ip=$(terraform output -raw instance_public_ip)\" >> \"$GITHUB_OUTPUT\"\
        \n"
      working-directory: infra
    - name: Wait for Vault to be reachable
      run: "curl --fail \\\n  --retry 15 \\\n  --retry-delay 15 \\\n  --retry-all-errors\
        \ \\\n  \"http://${{ steps.tf_ip.outputs.instance_ip }}:8200/v1/sys/health?standbyok=true&uninitcode=200&sealedcode=200\"\
        \n"
    - name: Print Vault UI URL
      run: echo "Vault UI -> http://${{ steps.tf_ip.outputs.instance_ip }}:8200/ui"
